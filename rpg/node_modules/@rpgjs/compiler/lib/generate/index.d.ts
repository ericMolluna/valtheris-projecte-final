/**
 * Generate the specified RPG module files and structure.
 *
 * @param type module type
 * @param directory directory name
 */
export declare function generate(type: string, directory: string): void;
