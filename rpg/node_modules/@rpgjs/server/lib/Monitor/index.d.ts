import { EventEmitter } from '@rpgjs/common';
declare class Monitor extends EventEmitter {
    private monitors;
    private lastTime;
    loopMs: any;
    totalConnected: number;
    get status(): {
        loop: any;
        totalConnected: number;
    };
    getStatusOf(id: any): any;
    update(server: any): void;
    addMonitor(socket: any): void;
    removeMonitor(id: any): void;
}
declare const _default: Monitor;
export default _default;
