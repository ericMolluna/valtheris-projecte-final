import { Log } from './log';
export declare class ItemLog {
    static notInInventory(itemClass: any): Log;
    static notUseItem(itemClass: any): Log;
    static chanceToUseFailed(itemClass: any): Log;
    static invalidToEquiped(itemClass: any): Log;
    static canNotEquip(itemClass: any): Log;
    static isAlreadyEquiped(itemClass: any): Log;
    static haveNotPrice(itemClass: any): Log;
    static notEnoughGold(itemClass: any, nb: any): Log;
    static tooManyToSell(itemClass: any, nbToSell: any, nb: any): Log;
    static restriction(itemClass: any): Log;
}
