export declare class Collection<T> {
    private collectionClass?;
    collection: Map<string, T>;
    constructor(collectionClass?: any);
    private removeCb;
    private addCb;
    registerRemoveCallback(cb: any): void;
    registerAddCallback(cb: any): void;
    detectChanges(data: {
        [id: string]: any;
    }): {};
}
