export declare class CustomError extends Error {
    private customMessage;
    status: number;
    code: string;
    message: string;
    constructor(customMessage: any);
    toObject(): {
        message: any;
        status: number;
        code: string;
    };
}
