export type MessageBuffer = [string, number, any];
export declare class Packet {
    private data;
    private roomId;
    constructor(data: any, roomId: string);
    get body(): any;
    message(otherData?: any): MessageBuffer;
    clone(data: any): Packet;
    encode(otherData?: any): any;
}
